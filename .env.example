# Copy to .env only for local tooling. Never commit real secrets.
POSTGRES_DB=campuscore
POSTGRES_USER=campuscore
POSTGRES_PASSWORD=replace-with-local-password
JWT_KEY=replace-with-at-least-32-random-characters
JWT_ISSUER=CampusCore
JWT_AUDIENCE=CampusCore.Web
CORS_ORIGINS=http://localhost:5173
