import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.sanskar.campuscore',
  appName: 'CampusCore',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
